{
  "version": 2,
  "builds": [
    {
      "src": "lexical-setup-guide/package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "dist"
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/lexical-setup-guide/$1"
    }
  ]
}
